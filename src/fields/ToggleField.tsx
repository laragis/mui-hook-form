import React from 'react'

const ToggleField = React.forwardRef(function ToggleField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default ToggleField
