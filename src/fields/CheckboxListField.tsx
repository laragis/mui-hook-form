import React from 'react'

const CheckboxListField = React.forwardRef(function CheckboxListField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default CheckboxListField
