import React from 'react'

const SliderField = React.forwardRef(function SliderField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default SliderField
