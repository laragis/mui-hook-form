import React from 'react'

const RadioListField = React.forwardRef(function RadioListField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default RadioListField
