import React from 'react'

// select, mutiple select

const SelectField = React.forwardRef(function SelectField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default SelectField
