import React from 'react'

const TextareaField = React.forwardRef(function TextareaField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default TextareaField
