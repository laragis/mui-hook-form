import React from 'react'

// file, muti-file

const FileField = React.forwardRef(function FileField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default FileField
