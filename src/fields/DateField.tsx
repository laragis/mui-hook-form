import React from 'react'

// mode: date, datetime, time, range

const DateField = React.forwardRef(function DateField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default DateField
