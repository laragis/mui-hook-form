import React from 'react'

const AutocompleteField = React.forwardRef(function AutocompleteField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default AutocompleteField
