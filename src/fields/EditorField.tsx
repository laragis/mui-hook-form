import React from 'react'

const EditorField = React.forwardRef(function EditorField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default EditorField
