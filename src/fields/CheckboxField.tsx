import React from 'react'

const CheckboxField = React.forwardRef(function CheckboxField(inProps, ref: any) {
  const rootProps = {
    ...inProps,
    ref
  }

  return null
})

export default CheckboxField
